import React from 'react';
import styled from "styled-components";
import Image from "next/image";

const Navbar = ({ tab, handleClick }) => {
    return (
        <div style={{ background: "#001529", color: "#d6dadd", display: "flex", justifyContent: "center", alignItems: "center", height: "60px", fontSize: "11pt", width: '100%', borderBottom:"2px solid #001529" }}>

        </div>
    );
}

export default Navbar;